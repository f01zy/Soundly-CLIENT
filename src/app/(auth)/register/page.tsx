import Register from "@/page/Register";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: `Register | Soundly`
}

const RegisterPage = () => {
  return <Register />
}

export default RegisterPage;